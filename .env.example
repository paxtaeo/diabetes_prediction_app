# ============================================================================
# Diabetes Prediction App - Environment Configuration Template
# ============================================================================
#
# INSTRUCTIONS FOR STUDENTS:
# -------------------------
# 1. Copy this file and rename it to ".env" (without the .example extension)
# 2. Fill in your actual values below
# 3. NEVER commit the .env file to version control (it's in .gitignore)
# 4. Keep your tokens and secrets secure
#
# To create your .env file from this template, run:
#   cp .env.example .env
#
# Then edit the .env file with your actual configuration values.
# ============================================================================


# ============================================================================
# REQUIRED: Databricks / MLflow Configuration
# ============================================================================

# Your Databricks personal access token
# IMPORTANT: This is sensitive! Never share this token or commit it to git.
#
# How to get your Databricks token:
# 1. Log into your Databricks workspace
# 2. Click on your user profile (top right)
# 3. Go to "User Settings"
# 4. Click "Developer" -> "Access tokens"
# 5. Click "Generate new token"
# 6. Copy the token and paste it below
DATABRICKS_TOKEN=your_databricks_token_here

# Your MLflow model serving endpoint URL
# This URL points to your deployed model in Databricks
#
# How to find your endpoint URL:
# 1. Go to your Databricks workspace
# 2. Navigate to "Serving" in the left sidebar
# 3. Click on your model endpoint (e.g., "diabetes_regressor")
# 4. Copy the "Endpoint URL" or "Invocation URL"
# 5. Paste it below
MLFLOW_ENDPOINT_URL=https://your-workspace.cloud.databricks.com/serving-endpoints/your-model-name/invocations


# ============================================================================
# OPTIONAL: Flask Application Configuration
# ============================================================================

# Flask secret key for session management
# Generate a secure random key for production using:
#   python -c "import secrets; print(secrets.token_hex(32))"
# SECRET_KEY=your_secret_key_here

# Enable or disable debug mode (True/False)
# Debug mode provides detailed error messages and auto-reloading
# Set to False in production!
# FLASK_DEBUG=True

# The host address to bind the Flask server to
# Use 0.0.0.0 to allow external connections
# Use 127.0.0.1 for local-only access
# FLASK_HOST=0.0.0.0

# The port on which the Flask application will run
# FLASK_PORT=4000


# ============================================================================
# OPTIONAL: Advanced Configuration
# ============================================================================

# Timeout for API requests to MLflow endpoint (in seconds)
# REQUEST_TIMEOUT=30

# Application name for display purposes
# APP_NAME=Diabetes Progression Predictor

# Application version
# APP_VERSION=1.0.0


# ============================================================================
# NOTES FOR STUDENTS
# ============================================================================
#
# Environment Variables Priority:
# -------------------------------
# The application loads configuration in this order:
# 1. Environment variables set in your shell
# 2. Variables defined in the .env file
# 3. Default values in config.py
#
# For example, if you set FLASK_PORT=5000 in your .env file,
# but also run: export FLASK_PORT=8080
# The application will use port 8080 (shell variable takes precedence)
#
# Best Practices:
# --------------
# - Use the .env file for local development
# - Use environment variables for production deployment
# - Never commit sensitive data (tokens, passwords) to version control
# - Use strong, random values for SECRET_KEY in production
# - Keep your .env file backed up securely (but not in git!)
#
# Security Tips:
# -------------
# - Rotate your Databricks token periodically
# - Use different tokens for development and production
# - Limit token permissions to only what's necessary
# - Monitor token usage in your Databricks workspace
# - If a token is compromised, revoke it immediately and generate a new one
#
# ============================================================================
